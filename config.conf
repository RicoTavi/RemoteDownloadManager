# 🔧 Remote Server Download Manager Configuration
# Edit these values to match your setup

# ═══════════════════════════════════════════════════════════
# 🌐 REMOTE SERVER SETTINGS
# ═══════════════════════════════════════════════════════════

# SSH username for the remote server
REMOTE_USER="aktiko820"

# Remote server hostname or IP address
REMOTE_HOST="zeus.bysh.me"

# Base path on remote server where files are located
# This is where the browser will start
REMOTE_BASE_PATH="/home/hd30/aktiko820/torrents/data/"

# Path to your SSH private key
# Default is usually ~/.ssh/id_rsa
SSH_KEY_PATH="~/.ssh/id_zeus"

# ═══════════════════════════════════════════════════════════
# 💾 NAS DOWNLOAD PATHS
# ═══════════════════════════════════════════════════════════

# Configure up to 5 quick-access download destinations
# Add or remove paths as needed

# Download Path 1
DOWNLOAD_PATH_1="/mnt/media_rw/c1a3015d-51a4-4bfa-a8f4-36df7d63267b/Share/Media/Spicy"
PATH_1_NAME="📥 Downloads"

# Download Path 2
DOWNLOAD_PATH_2="/mnt/media_rw/c1a3015d-51a4-4bfa-a8f4-36df7d63267b/Share/Media/DropZone"
PATH_2_NAME="🎬 Media"

# Download Path 3
DOWNLOAD_PATH_3="/volume1/backups"
PATH_3_NAME="💾 Backups"

# Download Path 4 (optional - uncomment to use)
#DOWNLOAD_PATH_4="/volume1/documents"
#PATH_4_NAME="📄 Documents"

# Download Path 5 (optional - uncomment to use)
#DOWNLOAD_PATH_5="/volume1/archive"
#PATH_5_NAME="📦 Archive"

# ═══════════════════════════════════════════════════════════
# ⚙️ ADVANCED SETTINGS
# ═══════════════════════════════════════════════════════════

# LFTP connection timeout (seconds)
CONNECTION_TIMEOUT=30

# Number of parallel downloads (1-10)
PARALLEL_DOWNLOADS=3

# Enable verbose logging (true/false)
VERBOSE_LOGGING=false

# Log file location
LOG_FILE="logs/download-history.log"

# ═══════════════════════════════════════════════════════════
# 🎨 INTERFACE SETTINGS
# ═══════════════════════════════════════════════════════════

# Show file sizes in listings (true/false)
SHOW_FILE_SIZES=true

# Show file modification dates (true/false)
SHOW_FILE_DATES=false

# Show folder sizes in listings (true/false)
# Note: Calculating folder sizes can be slow for large directories
# When enabled, sizes are cached for 5 minutes for fast subsequent views
# Set to 'false' for faster browsing (recommended default)
SHOW_FOLDER_SIZES=false

# Maximum number of files to display per page
MAX_FILES_PER_PAGE=20

# ═══════════════════════════════════════════════════════════
# 📝 NOTES
# ═══════════════════════════════════════════════════════════
#
# - All paths should be absolute paths (start with /)
# - Make sure the SSH key has proper permissions (chmod 600)
# - Test your SSH connection before using this tool:
#   ssh -i ~/.ssh/id_rsa username@hostname
# - The logs directory will be created automatically
#
# ═══════════════════════════════════════════════════════════
# Number of parallel chunks for downloads (IDM-style)
# Higher = faster for large files (recommended: 8-16)
DOWNLOAD_CHUNKS=8
